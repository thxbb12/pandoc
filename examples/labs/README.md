Example of what can be done with pandoc + latex to create practical labs along corrections.

Simply type `make` in the current directory to produce `example.pdf` and example_corr.pdf` from `example.md`.
